export interface ISSLCommerz {
  // this is available in db for that we are using this interface
  amount: number;
  transactionId: string;
  name: string;
  email: string;
  phoneNumber: string;
  address: string;
}
